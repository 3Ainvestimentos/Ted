rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Regra Global: Permite leitura e escrita em qualquer documento
    // somente se o usuário estiver autenticado via Firebase Authentication.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
